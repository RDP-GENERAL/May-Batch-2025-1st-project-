create database may_python_day_1_login;
use may_python_day_1_login;
show databases;

CREATE TABLE basic_details (
    c_id INT AUTO_INCREMENT PRIMARY KEY,
    full_name VARCHAR(100) NOT NULL,
    address VARCHAR(255) NOT NULL,
    ph_no BIGINT NOT NULL,
    user_id VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(100) NOT NULL
) AUTO_INCREMENT = 100;

select * from basic_details;

INSERT INTO basic_details (full_name, address, ph_no, user_id, password)
VALUES (
    'Rajdeep Bhadra',
    '23A Lake Road, Kolkata, West Bengal - 700029',
    9830012345,
    'rajdeep.bhadra',
    'Secure@123'
);

select * from basic_details where user_id='raj123';
select * from basic_details WHERE user_id=raj123;
